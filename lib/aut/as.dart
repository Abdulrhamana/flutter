import 'dart:async';

import 'package:flutter/material.dart';


class myya extends StatefulWidget {
  const myya({Key? key}) : super(key: key);

  @override
  State<myya> createState() => _myyaState();
  
}

class _myyaState extends State<myya> {
  
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Container(
        color:Color.fromARGB(88, 11,22, 22),
        child:Column(children: [
 Text("sdadda")
        ],)
      ),
    );
  }
}
